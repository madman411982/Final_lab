# Запуск проекта:
sudo docker-compose up --build
# Важно! Перед запуском проекта выполнить команду, чтобы выдать разрешения:
sudo chmod 600 .erlang.cookie!
# В Графане содать пользователя Admin Admin и настроить Datasource http://prometheus:9090 
